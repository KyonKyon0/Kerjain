{
  "name": "kerjain-monorepo",
  "version": "1.0.0",
  "description": "Unified runner for Kerjain Frontend & Backend",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
